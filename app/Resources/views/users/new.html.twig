{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1> {{ 'createuser'|trans }} </h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{  'create'|trans }}" />
    {{ form_end(form) }}

    <ul>
        <li class="btn btn-light">
            <a href="{{ path('users_index') }}"> {{ 'backlist'|trans }} </a>
        </li>
    </ul>
</div>
{% endblock %}
